Password Level 3
Add environment variables to store
1. npm install dotenv
2. add this to the top of your file (app.js)
require('dotenv').config()
3. Create .env file in root(use cli and then run ls -a to see the file)
4. Add variables using the following format ( NAME = VALUE)
  ex. SECRET = longstringasasecret.
  API_KEY = jweijjfojfjrkfpokfgo
5. in app.js add the following process.env
